name: Build & Push

on:
  push:
    branches: [qa]

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/landtaxdispute-server-qa:latest
            ghcr.io/${{ github.repository_owner }}/landtaxdispute-server-qa:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
