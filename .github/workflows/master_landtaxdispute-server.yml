name: Build and deploy Node.js app to Azure Web App - landtaxdispute-server

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '24.x'

      - name: Install and build
        run: |
          npm ci
          npm run build

      - name: Run database migrations
        env:
          NODE_ENV: production
          DB_HOST:  ${{ github.ref == 'refs/heads/main' && vars.DB_HOST_PROD || github.ref == 'refs/heads/qa' && vars.DB_HOST_QA || vars.DB_HOST_DEV }}
          DB_PORT: ${{ github.ref == 'refs/heads/main' && vars.DB_PORT_PROD || github.ref == 'refs/heads/qa' && vars.DB_PORT_QA || vars.DB_PORT_DEV }}
          DB_USERNAME: ${{ github.ref == 'refs/heads/main' && vars.DB_USERNAME_PROD || github.ref == 'refs/heads/qa' && vars.DB_USERNAME_QA || vars.DB_USERNAME_DEV }}
          DB_PASSWORD: ${{ github.ref == 'refs/heads/main' && vars.DB_PASSWORD_PROD || github.ref == 'refs/heads/qa' && vars.DB_PASSWORD_QA || vars.DB_PASSWORD_DEV }}
          DB_NAME: ${{ github.ref == 'refs/heads/main' && vars.DB_NAME_PROD || github.ref == 'refs/heads/qa' && vars.DB_NAME_QA || vars.DB_NAME_DEV }}
          DB_SSL: ${{ github.ref == 'refs/heads/main' && vars.DB_SSL_PROD || github.ref == 'refs/heads/qa' && vars.DB_SSL_QA || vars.DB_SSL_DEV }}
        run: npm run migration:run:prod

      - name: Prune dev dependencies
        run: npm prune --omit=dev

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'landtaxdispute-server'
          slot-name: 'Production'
          package: .
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_A76461515FA9422A88808EE1E33679FD }}