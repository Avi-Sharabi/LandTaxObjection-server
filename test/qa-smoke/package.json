{
  "name": "qa-login-smoke",
  "version": "1.0.0",
  "private": true,
  "description": "Puppeteer login regression suite for QA — isolated from the main app's dependencies.",
  "scripts": {
    "test": "jest --runInBand",
    "test:debug": "cross-env HEADLESS=true jest --runInBand"
  },
  "dependencies": {
    "puppeteer": "^24.43.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^17.4.2",
    "jest": "^29.7.0",
    "jest-puppeteer": "^10.1.4"
  }
}
